namespace Terminal
{
    internal static class Program
    {
        private static void Main()
        {
            var session = new Session();
            session.Execute();
        }
    }
}
